<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="HackatonProject.Views.ProfileView"
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:viewModes="clr-namespace:HackatonProject.ViewModels">
    <ContentPage.BindingContext>
        <viewModes:ProfileViewModel />
    </ContentPage.BindingContext>

    <StackLayout Margin="15">
        <StackLayout Orientation="Horizontal">
            <Label Text="Name - " TextColor="White" />
            <Label Text="{Binding User.Name}" TextColor="White" />
        </StackLayout>
        <StackLayout Orientation="Horizontal">
            <Label Text="Email - " TextColor="White" />
            <Label Text="{Binding User.Email}" TextColor="White" />
        </StackLayout>
        <StackLayout Orientation="Horizontal">
            <Label Text="Password - " TextColor="White" />
            <Label Text="{Binding User.Password}" TextColor="White" />
        </StackLayout>
    </StackLayout>
</ContentPage>